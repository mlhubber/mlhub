[setup]

including _common_setup.xly

[act]

@[ML]@ readme pytempl

[assert]

exit-code == 0

stdout equals <<EOF
MLHub Template and Test Package for Python

A test package for https://mlhub.ai based on Python.

To list the commands supported by the model package:

  $ ml commands pytempl

EOF

stderr is-empty
